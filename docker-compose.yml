services:
  todo.api:
    image: todo.api
    build:
      context: .
      dockerfile: Todo.API/Dockerfile
  image-resize.workerservice:
    image: image-process.workerservice
    build:
      context: .
      dockerfile: ImageProcess.WorkerService/Dockerfile
